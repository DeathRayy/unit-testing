pipeline {
        agent none
        stages {
         
          stage("build & SonarQube Scanner") {
            agent any
            steps {
              withSonarQubeEnv('Unit-test-sonar') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }